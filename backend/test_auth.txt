<?php>

return [
    /*
    |--------------------------------------------------------------------------
    | Default AI Provider
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Always configure provider failover in config/ai.php
    | ÔÇö never rely on a single provider.
    |
    */

    'default' => env('AI_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | AI Providers
    |--------------------------------------------------------------------------
    |
    | RULE AI-02: Tools used by agents are injected via DI
    | ÔÇö never instantiated inside the Agent.
    |
    */

    'providers' => [
        'openai' => [
            'driver' => 'openai',
            'api_key' => env('OPENAI_API_KEY'),
            'model' => env('OPENAI_MODEL', 'gpt-4'),
            'embedding_model' => env('OPENAI_EMBEDDING_MODEL', 'text-embedding-3-small'),
        ],

        'anthropic' => [
            'driver' => 'anthropic',
            'api_key' => env('ANTHROPIC_API_KEY'),
            'model' => env('ANTHROPIC_MODEL', 'claude-3-opus'),
        ],

        'gemini' => [
            'driver' => 'gemini',
            'api_key' => env('GEMINI_API_KEY'),
            'model' => env('GEMINI_MODEL', 'gemini-pro'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Failover
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Configure failover ÔÇö never rely on single provider.
    |
    */

    'failover' => ['openai', 'anthropic', 'gemini'],

    /*
    |--------------------------------------------------------------------------
    | Fake Provider for Testing
    |--------------------------------------------------------------------------
    |
    | RULE AI-08: Test AI features using fake providers
    | ÔÇö never call real AI APIs in tests.
    |
    */

    'testing' => [
        'driver' => 'fake',
    ],
];
<?php>

return [
    /*
    |--------------------------------------------------------------------------
    | Default AI Provider
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Always configure provider failover in config/ai.php
    | ÔÇö never rely on a single provider.
    |
    */

    'default' => env('AI_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | AI Providers
    |--------------------------------------------------------------------------
    |
    | RULE AI-02: Tools used by agents are injected via DI
    | ÔÇö never instantiated inside the Agent.
    |
    */

    'providers' => [
        'openai' => [
            'driver' => 'openai',
            'api_key' => env('OPENAI_API_KEY'),
            'model' => env('OPENAI_MODEL', 'gpt-4'),
            'embedding_model' => env('OPENAI_EMBEDDING_MODEL', 'text-embedding-3-small'),
        ],

        'anthropic' => [
            'driver' => 'anthropic',
            'api_key' => env('ANTHROPIC_API_KEY'),
            'model' => env('ANTHROPIC_MODEL', 'claude-3-opus'),
        ],

        'gemini' => [
            'driver' => 'gemini',
            'api_key' => env('GEMINI_API_KEY'),
            'model' => env('GEMINI_MODEL', 'gemini-pro'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Failover
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Configure failover ÔÇö never rely on single provider.
    |
    */

    'failover' => ['openai', 'anthropic', 'gemini'],

    /*
    |--------------------------------------------------------------------------
    | Fake Provider for Testing
    |--------------------------------------------------------------------------
    |
    | RULE AI-08: Test AI features using fake providers
    | ÔÇö never call real AI APIs in tests.
    |
    */

    'testing' => [
        'driver' => 'fake',
    ],
];
<?php>

return [
    /*
    |--------------------------------------------------------------------------
    | Default AI Provider
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Always configure provider failover in config/ai.php
    | ÔÇö never rely on a single provider.
    |
    */

    'default' => env('AI_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | AI Providers
    |--------------------------------------------------------------------------
    |
    | RULE AI-02: Tools used by agents are injected via DI
    | ÔÇö never instantiated inside the Agent.
    |
    */

    'providers' => [
        'openai' => [
            'driver' => 'openai',
            'api_key' => env('OPENAI_API_KEY'),
            'model' => env('OPENAI_MODEL', 'gpt-4'),
            'embedding_model' => env('OPENAI_EMBEDDING_MODEL', 'text-embedding-3-small'),
        ],

        'anthropic' => [
            'driver' => 'anthropic',
            'api_key' => env('ANTHROPIC_API_KEY'),
            'model' => env('ANTHROPIC_MODEL', 'claude-3-opus'),
        ],

        'gemini' => [
            'driver' => 'gemini',
            'api_key' => env('GEMINI_API_KEY'),
            'model' => env('GEMINI_MODEL', 'gemini-pro'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Failover
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Configure failover ÔÇö never rely on single provider.
    |
    */

    'failover' => ['openai', 'anthropic', 'gemini'],

    /*
    |--------------------------------------------------------------------------
    | Fake Provider for Testing
    |--------------------------------------------------------------------------
    |
    | RULE AI-08: Test AI features using fake providers
    | ÔÇö never call real AI APIs in tests.
    |
    */

    'testing' => [
        'driver' => 'fake',
    ],
];
<?php>

return [
    /*
    |--------------------------------------------------------------------------
    | Default AI Provider
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Always configure provider failover in config/ai.php
    | ÔÇö never rely on a single provider.
    |
    */

    'default' => env('AI_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | AI Providers
    |--------------------------------------------------------------------------
    |
    | RULE AI-02: Tools used by agents are injected via DI
    | ÔÇö never instantiated inside the Agent.
    |
    */

    'providers' => [
        'openai' => [
            'driver' => 'openai',
            'api_key' => env('OPENAI_API_KEY'),
            'model' => env('OPENAI_MODEL', 'gpt-4'),
            'embedding_model' => env('OPENAI_EMBEDDING_MODEL', 'text-embedding-3-small'),
        ],

        'anthropic' => [
            'driver' => 'anthropic',
            'api_key' => env('ANTHROPIC_API_KEY'),
            'model' => env('ANTHROPIC_MODEL', 'claude-3-opus'),
        ],

        'gemini' => [
            'driver' => 'gemini',
            'api_key' => env('GEMINI_API_KEY'),
            'model' => env('GEMINI_MODEL', 'gemini-pro'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Failover
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Configure failover ÔÇö never rely on single provider.
    |
    */

    'failover' => ['openai', 'anthropic', 'gemini'],

    /*
    |--------------------------------------------------------------------------
    | Fake Provider for Testing
    |--------------------------------------------------------------------------
    |
    | RULE AI-08: Test AI features using fake providers
    | ÔÇö never call real AI APIs in tests.
    |
    */

    'testing' => [
        'driver' => 'fake',
    ],
];
<?php>

return [
    /*
    |--------------------------------------------------------------------------
    | Default AI Provider
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Always configure provider failover in config/ai.php
    | ÔÇö never rely on a single provider.
    |
    */

    'default' => env('AI_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | AI Providers
    |--------------------------------------------------------------------------
    |
    | RULE AI-02: Tools used by agents are injected via DI
    | ÔÇö never instantiated inside the Agent.
    |
    */

    'providers' => [
        'openai' => [
            'driver' => 'openai',
            'api_key' => env('OPENAI_API_KEY'),
            'model' => env('OPENAI_MODEL', 'gpt-4'),
            'embedding_model' => env('OPENAI_EMBEDDING_MODEL', 'text-embedding-3-small'),
        ],

        'anthropic' => [
            'driver' => 'anthropic',
            'api_key' => env('ANTHROPIC_API_KEY'),
            'model' => env('ANTHROPIC_MODEL', 'claude-3-opus'),
        ],

        'gemini' => [
            'driver' => 'gemini',
            'api_key' => env('GEMINI_API_KEY'),
            'model' => env('GEMINI_MODEL', 'gemini-pro'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Failover
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Configure failover ÔÇö never rely on single provider.
    |
    */

    'failover' => ['openai', 'anthropic', 'gemini'],

    /*
    |--------------------------------------------------------------------------
    | Fake Provider for Testing
    |--------------------------------------------------------------------------
    |
    | RULE AI-08: Test AI features using fake providers
    | ÔÇö never call real AI APIs in tests.
    |
    */

    'testing' => [
        'driver' => 'fake',
    ],
];
<?php>

return [
    /*
    |--------------------------------------------------------------------------
    | Default AI Provider
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Always configure provider failover in config/ai.php
    | ÔÇö never rely on a single provider.
    |
    */

    'default' => env('AI_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | AI Providers
    |--------------------------------------------------------------------------
    |
    | RULE AI-02: Tools used by agents are injected via DI
    | ÔÇö never instantiated inside the Agent.
    |
    */

    'providers' => [
        'openai' => [
            'driver' => 'openai',
            'api_key' => env('OPENAI_API_KEY'),
            'model' => env('OPENAI_MODEL', 'gpt-4'),
            'embedding_model' => env('OPENAI_EMBEDDING_MODEL', 'text-embedding-3-small'),
        ],

        'anthropic' => [
            'driver' => 'anthropic',
            'api_key' => env('ANTHROPIC_API_KEY'),
            'model' => env('ANTHROPIC_MODEL', 'claude-3-opus'),
        ],

        'gemini' => [
            'driver' => 'gemini',
            'api_key' => env('GEMINI_API_KEY'),
            'model' => env('GEMINI_MODEL', 'gemini-pro'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Failover
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Configure failover ÔÇö never rely on single provider.
    |
    */

    'failover' => ['openai', 'anthropic', 'gemini'],

    /*
    |--------------------------------------------------------------------------
    | Fake Provider for Testing
    |--------------------------------------------------------------------------
    |
    | RULE AI-08: Test AI features using fake providers
    | ÔÇö never call real AI APIs in tests.
    |
    */

    'testing' => [
        'driver' => 'fake',
    ],
];
<?php>

return [
    /*
    |--------------------------------------------------------------------------
    | Default AI Provider
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Always configure provider failover in config/ai.php
    | ÔÇö never rely on a single provider.
    |
    */

    'default' => env('AI_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | AI Providers
    |--------------------------------------------------------------------------
    |
    | RULE AI-02: Tools used by agents are injected via DI
    | ÔÇö never instantiated inside the Agent.
    |
    */

    'providers' => [
        'openai' => [
            'driver' => 'openai',
            'api_key' => env('OPENAI_API_KEY'),
            'model' => env('OPENAI_MODEL', 'gpt-4'),
            'embedding_model' => env('OPENAI_EMBEDDING_MODEL', 'text-embedding-3-small'),
        ],

        'anthropic' => [
            'driver' => 'anthropic',
            'api_key' => env('ANTHROPIC_API_KEY'),
            'model' => env('ANTHROPIC_MODEL', 'claude-3-opus'),
        ],

        'gemini' => [
            'driver' => 'gemini',
            'api_key' => env('GEMINI_API_KEY'),
            'model' => env('GEMINI_MODEL', 'gemini-pro'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Failover
    |--------------------------------------------------------------------------
    |
    | RULE AI-03: Configure failover ÔÇö never rely on single provider.
    |
    */

    'failover' => ['openai', 'anthropic', 'gemini'],

    /*
    |--------------------------------------------------------------------------
    | Fake Provider for Testing
    |--------------------------------------------------------------------------
    |
    | RULE AI-08: Test AI features using fake providers
    | ÔÇö never call real AI APIs in tests.
    |
    */

    'testing' => [
        'driver' => 'fake',
    ],
];

   FAIL  Tests\Feature\AuthApiTest
  Ô£ô guest can register                                                                                           3.70s  
  Ô£ô registration fails with invalid email                                                                        0.15s  
  Ô£ô guest can login with valid credentials                                                                       0.10s  
  Ô£ô login fails with invalid password                                                                            0.09s  
  Ô£ô login fails for unverified email                                                                             0.06s  
  Ô¿» can verify email with valid token                                                                            0.09s  
  Ô£ô cannot access protected routes without token                                                                 0.07s  
  ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ  
   FAILED  Tests\Feature\AuthApiTest > can verify email with valid token                                                
  Expected response status code [200] but received 422.
Failed asserting that 422 is identical to 200.

The following errors occurred during the last request:

{
    "message": "The token field is required.",
    "errors": {
        "token": [
            "The token field is required."
        ]
    }
}

  at tests\Feature\AuthApiTest.php:117
    113Ôûò         $response = $this->postJson('/api/auth/verify-email', [
    114Ôûò             'token' => $verificationToken,
    115Ôûò         ]);
    116Ôûò 
  Ô×£ 117Ôûò         $response->assertStatus(200);
    118Ôûò         $this->assertNotNull($guest->fresh()->email_verified_at);
    119Ôûò     }
    120Ôûò 
    121Ôûò     public function test_cannot_access_protected_routes_without_token()


  Tests:    1 failed, 6 passed (17 assertions)
  Duration: 4.72s

